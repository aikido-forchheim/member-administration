<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms" prism:ViewModelLocator.AutowireViewModel="True" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="MemberAdministration.EnterPasswordPage">
	<ContentPage.Content>
		<StackLayout VerticalOptions="Center" HorizontalOptions="Center">
			<Grid VerticalOptions="Center" HorizontalOptions="Center">
				<Grid.ColumnDefinitions>
					<ColumnDefinition>
					</ColumnDefinition>
					<ColumnDefinition>
					</ColumnDefinition>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition>
					</RowDefinition>
					<RowDefinition>
					</RowDefinition>
					<RowDefinition>
					</RowDefinition>
				</Grid.RowDefinitions>
			</Grid>

			<Label Grid.Row="0" Grid.Column="0" Text="Neues Passwort"></Label>

			<Entry Grid.Row="0" Grid.Column="1" Text="{Binding Password}" IsPassword="true"></Entry>

			<Label Grid.Row="1" Grid.Column="0" Text="Passwort wiederholen:"></Label>

			<Entry Grid.Row="1" Grid.Column="1" Text="{Binding PasswordRepeat}" IsPassword="true"></Entry>

			<Button Text="Neues Passwort speichern" Command="{Binding SavePasswordCommand}"></Button>
			
		</StackLayout>
	</ContentPage.Content>
</ContentPage>